import { TresObject } from '../types';

export declare const vRotate: {
    mounted: (el: TresObject, binding: {
        arg: 'x' | 'y' | 'z';
        value: number;
        modifiers: Partial<{
            x: boolean;
            y: boolean;
            z: boolean;
        }>;
    }) => void;
};
